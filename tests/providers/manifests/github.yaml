# Provider manifest: github (P3-T14)
# Matrix row: docs/implementation/provider-matrix.yaml -> provider_id: github
provider_id: github
provider_type: github_models
auth_type: oauth
credential_env_var: GOROUTER_LIVE_GITHUB_KEY
timeout: 30s
retries: 2
mock_fixtures:
  FormatOpenAICompat: tests/providers/harness/testdata/fixtures/openai_compat.json
