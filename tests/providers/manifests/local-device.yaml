# Provider manifest: local-device (P3-T14)
# Matrix row: docs/implementation/provider-matrix.yaml -> provider_id: local-device
provider_id: local-device
provider_type: local_device
auth_type: none
timeout: 30s
retries: 2
mock_fixtures:
  FormatOpenAICompat: tests/providers/harness/testdata/fixtures/openai_compat.json
