# Provider manifest: cline (P3-T14)
# Matrix row: docs/implementation/provider-matrix.yaml -> provider_id: cline
provider_id: cline
provider_type: cline
auth_type: oauth
credential_env_var: GOROUTER_LIVE_CLINE_KEY
timeout: 30s
retries: 2
mock_fixtures:
  FormatOpenAICompat: tests/providers/harness/testdata/fixtures/openai_compat.json
