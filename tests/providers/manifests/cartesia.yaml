# Provider manifest: cartesia (P3-T14)
# Matrix row: docs/implementation/provider-matrix.yaml -> provider_id: cartesia
provider_id: cartesia
provider_type: cartesia
auth_type: apikey
credential_env_var: GOROUTER_LIVE_CARTESIA_KEY
timeout: 30s
retries: 2
mock_fixtures:
  FormatOpenAICompat: tests/providers/harness/testdata/fixtures/openai_compat.json
