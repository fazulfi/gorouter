# Provider manifest: gitlab (P3-T14)
# Matrix row: docs/implementation/provider-matrix.yaml -> provider_id: gitlab
provider_id: gitlab
provider_type: gitlab
auth_type: oauth
credential_env_var: GOROUTER_LIVE_GITLAB_KEY
timeout: 30s
retries: 2
mock_fixtures:
  FormatOpenAICompat: tests/providers/harness/testdata/fixtures/openai_compat.json
