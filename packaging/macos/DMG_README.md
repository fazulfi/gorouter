# Gorouter macOS package

The app bundle uses the menu-bar agent (`LSUIElement`) and does not configure autostart.
Developer ID signing and notarization are pipeline-only steps; local key material is intentionally absent.
