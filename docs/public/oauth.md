# OAuth

Configure an OAuth provider with its issuer URL, client ID, and secret supplied through the deployment secret store. Redirect URIs must match the registered provider application exactly.

Test login and logout in a non-production environment before deployment.