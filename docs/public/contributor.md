# Contributing

Run `gofmt`, `go test ./...`, and `git diff --check` before submitting changes. Add regression tests for behavior changes and keep examples free of credentials.

Documentation changes should update the relevant public guide and preserve English/Indonesian catalog parity.