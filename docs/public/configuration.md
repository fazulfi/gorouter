# Configuration

Set `DATABASE_URL` for PostgreSQL and configure providers through the admin API. Store API keys in the deployment secret store, not in source files or examples.

The service listens on its configured HTTP address. Review [security validation](deployment-checklist.md#security-validation) before production use.