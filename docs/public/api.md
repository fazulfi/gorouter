# API Reference

Public health endpoint: `GET /healthz`.

Admin endpoints require authentication and follow the JSON request/response contracts in the transport package. Use `GET /api/v1/healthz` for an authenticated service check. Do not include credentials in request examples.