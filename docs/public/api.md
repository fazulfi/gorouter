# API Reference

Public health endpoint: `GET /health`.

Admin endpoints require authentication and follow the JSON request/response contracts in the transport package. Use `GET /api/admin/v1/health` for an authenticated service check. Do not include credentials in request examples.