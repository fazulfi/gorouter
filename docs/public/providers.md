# Providers

Providers are configured independently from routing. Keep provider credentials in the secret store and validate connectivity before enabling production traffic.

See the [API reference](api.md) for health checks and [configuration](configuration.md) for environment setup.