# API-02 Evidence

## 1. Task scope and lens
Contract generator, regenerated Admin API client, and fail-closed diff gate.

## 2. Sources inspected
- `.sisyphus/evidence/api-01.md` — completed 119-path/153-operation contract.
- `.sisyphus/plans/phase-4-lane-api-compat-realtime.md:43-60` — API-02 definition.
- `.sisyphus/plans/phase-4-design.md` sections 7, 13, and 15 — API contract, VPS-only verification, and generator ownership.
- `api/admin-v1.openapi.yaml` — HEAD contract input.

## 3. Findings and decisions
`tools/contractgen` deterministically parses OpenAPI YAML schemas, request bodies, path parameters, responses, and operations into TypeScript types and typed fetch operations using `/api/admin/v1`. `--check` compares generated bytes with the committed client and exits non-zero on drift. CI runs the same check before frontend typecheck.

## 4. Risks, conflicts, assumptions, unresolved questions
The generator intentionally supports the contract constructs required by the completed Admin API surface; future contract constructs must extend the generator and regenerate the client in the same change. No API YAML or backend files were modified.

## 5. Verification performed
Exact VPS sync command: `C:/Users/faizz/AppData/Local/Temp/opencode/vps-sync.sh C:/Users/faizz/AppData/Local/Temp/opencode/gorouter-phase4`; remote `/var/tmp/phase4-sync-20260804-222228`, user `gorouter`, host `159.65.10.135`, `TMPDIR=/var/tmp`.

VPS GREEN output:
```
Generated frontend/src/generated/admin-v1.ts from /var/tmp/phase4-sync-20260804-222228/api/admin-v1.openapi.yaml
PASS: frontend/src/generated/admin-v1.ts matches regenerated output
GREEN
```

VPS checks passed: `gofmt -w tools/contractgen/*.go`, `go run ./tools/contractgen`, `go run ./tools/contractgen --check`, `go build ./tools/contractgen/...`, `go vet ./tools/contractgen/...`, and empty `gofmt -l tools/contractgen/`.

RED/GREEN behavior: the fail-closed gate compares generated output byte-for-byte; stale output exits 1, regenerated output exits 0. The committed client was regenerated on the VPS and then copied back.

## 6. Verdict
**PASS** — API-02 deliverables implemented and VPS-verified.

## 7. Recommended next actions
Keep the OpenAPI contract and generated client synchronized through the CI diff gate; downstream API/frontend atoms may consume the generated types.
